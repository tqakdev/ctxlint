# CLAUDE.md — packages/web

Claude-specific notes for the storefront package (see AGENTS.md in this directory for
the general rules).

- When editing components, run the affected component's test file directly first; the
  full web suite takes several minutes.
- Screenshot-based review: after visual changes, describe what changed rather than
  assuming the reviewer will diff the pixels.
